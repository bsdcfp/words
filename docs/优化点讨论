1. 单词书增加雅思，暂不考虑托福
2. 词汇量测试
  * 入学测
  * 阶段测
3. 背诵过程
  * S1:45个单词，一次性选完。照相功能，参照 5 倍速阅读法
  * 单词识记：循环播放 3/6/9 /无限，直到点击（已记住），点击还不熟（再次播放 3/6/9），去掉播放按钮；如果没有听到跟读，提示学生跟读（可以手动关闭这个）
  * 本组复习：仅保留单词和音标，2s 内没有点击记住，就自动出现词性和中文释义。
   （原则：快感：速度，直观：一目了然）
   * 听音辨义：播放英文、语音识别中文释义和词性，或者词性和中文释义
   * 再来一次本组复习：看中文，回忆英文。2s 内没有点击记住，就自动出现英文单词。
   * 申请暂停
   * 申请今日完成，给一个调整计划提示，需要申请老师介入吗？
   * AI Agent 老师，Token费用分摊到每个用户
   * 复习节奏：
    * 1 组 复习一次
    * 2 组 复习一次，2+1
    * 3 组 复习一次，3+2
    * 3+1 复习一次
    1 组：3 次，2 组：3 次,3 组：3 次
    * A：1+2+3 组
    * B：1+2+3 组 
    A + B 混合。
    * C：1+2+3 组
    * D：1+2+3 组 
     C + D 混合。
    * E：1+2+3 组 
    * F：1+2+3 组 
    最后大组复习，排序
  * 错题本
    * 加入多元化记忆（同音、同源、图形、不记忆-下一轮色彩标注）
    * 把你认为适合你记忆的方式提交给我
    * 睡前复习一次，第二天早上复习一次
    * 第一个list 完成，上榜以及进度
  *  list完成标准
    * 默认：完成当天错题，第二天早上完成复习，奖励积分
    * 自定义 1：完成当天新词学习
  # 生态圈
  * 用户心得
  * 排行榜
  * 积分系统设计
  * 任务环

  # 平台化
  * ToB

---

# List 推进机制 v2（2026-05-23 讨论定稿）

## 1. 产品主目标

首要目标从“按艾宾浩斯遗忘曲线长期复习”调整为：

**完成一本单词书。**

产品主逻辑不再围绕艾宾浩斯排程展开，而是围绕：

1. 选择单词书 / 单词水平
2. 按 list 推进
3. 每个 list 内按 3 个词一组学习
4. 当天错词进入一个统一错词本
5. 睡前和第二天早上默认提醒错词复习
6. 用积分和进度鼓励继续完成单词书

## 2. List 配置

- 每组固定 3 个词。
- 一个 list 由若干组构成。
- list 长度是用户配置项。
- 默认 list 长度：15 组，也就是 45 个词。
- 配置入口放在“我的”里，和“选择单词水平 / 单词书配置”放在一起。
- list 可配置为 9 组、15 组等，后续可继续扩展。

## 3. List 暂停与隔天处理

### 当天暂停

- 学生可以主动暂停。
- 当天再次进入时，继续当前 list。

### 隔天回来

如果昨天 list 没完成：

- 不强制继续昨天 list。
- 今天开始新的 list。
- 昨天已经学过的新词不重复学习。
- 首页提示用户：昨天未完成，是否需要调小 list 长度。
- 昨天产生的错词是否复习，取决于用户的错词提醒策略。

## 4. 默认完成标准

默认完成标准：

**按分组学习法学完新词 + 睡前完成错词复习 + 第二天早上完成错词复习。**

用户也可以自定义完成标准，例如：

- 仅学完新词就算完成。
- 暂停错词提醒，只在错词本里自由复习。

## 5. 进度与状态

### 用户看到的状态

首页不展示复杂状态机，只显示“下一步该做什么”。

首页任务优先级：

1. 继续未完成 list
2. 睡前 / 晨间错词复习
3. 开始新 list

### 系统内部状态

系统内部记录 list 状态：

- 新词已完成
- 待睡前复习
- 待晨间复习
- 完整完成

### 单词书进度口径

- 新词学完，单词书进度就增加。
- 如果默认复习没完成，该 list 显示为“待巩固”。
- 睡前和晨间错词复习都完成后，升级为“完整完成”。

## 6. 奖励与积分

奖励拆成三段：

1. 新词学完：基础积分
2. 睡前错词复习完成：额外积分
3. 晨间错词复习完成：额外积分

完整完成 = 三段都完成。

这样既奖励推进单词书，也奖励错词复习闭环。

## 7. 睡前 / 晨间复习窗口

用户可以在“我的”里设置睡觉时间和起床时间。

默认值：

- 睡觉时间：22:00
- 起床时间：06:00

### 睡前复习窗口

- 从用户设置的睡觉时间前 2 小时开始。
- 到当天结束。
- 默认窗口：20:00-23:59。

### 晨间复习窗口

- 从用户设置的起床时间前 1 小时开始。
- 到起床时间后 3 小时结束。
- 默认窗口：05:00-09:00。

提醒原则：

- 到达窗口后，只在首页任务卡提示。
- 不强弹窗，不打断用户。

## 8. 错词本机制

### 错词本数量

学生端只有一个错词本。

### 错词来源

以下环节失败会进入错词本：

- 本组复习
- 听音辨义
- 看中文回忆英文

“还不熟”不算错词，只影响当前播放，会立即再播放一轮。

### 错词清除

- 错词复习时，点击“已记住”即可从错词本移除。
- 不区分语音验证通过 / 自评通过。
- 历史错题记录保留，但学生端不展示“曾错过”。
- 历史记录仅用于报告和后续策略。

### 错词排序

错词复习排序：

1. 当前 list 的错词优先
2. 再按出错时间排序

### 睡前 / 晨间提醒范围

- 提醒错词本里所有未清除错词。
- 排序仍然是当前 list 错词优先。

## 9. 错词提醒策略

默认策略：

- 睡前提醒一次。
- 第二天早上提醒一次。

提醒不阻塞开始新的 list。

如果用户连续不复习，触发调整策略提示：

- 连续 2 次错过睡前 / 晨间提醒
- 或错词本超过 30 个

任一触发后，提示用户调整错词复习策略。

当前确定的调整策略：

- 暂停错词提醒，只在错词本里自由复习。

暂停后：

- 不再反复打扰。
- 只能在“我的”里手动恢复提醒。

## 10. List 内默认学习流程

每组 3 个词，默认流程：

1. 单词识记
2. 本组复习
3. 听音辨义
4. 看中文回忆英文
5. 混组复习

每个环节都自动发音，并提示学生跟读。

允许两种使用方式在同一界面共存：

- 耳朵 + 眼睛 + 手：听音，看内容，点击“已记住”
- 耳朵 + 嘴巴：听音，跟读或语音作答

不单独拆分学习模式。

## 11. 混组复习节奏

### 小组与大 group

- 每个小组固定 3 个词。
- 每 3 个小组组成一个大的 group。
- 也就是说，一个大 group 默认包含 9 个词。

### 单个大 group 内的节奏

整体节奏是：

**本组复习 -> 听音辨义 -> 看中文回忆英文 -> 混组复习。**

在一个大 group 内，混组复习按下面节奏推进：

1. 第 1 组学完后：第 1 组单独复习一次。
2. 第 2 组学完后：第 2 组单独复习一次，然后第 2 组 + 第 1 组一起混组复习。
3. 第 3 组学完后：第 3 组单独复习一次，然后第 3 组 + 第 2 组一起混组复习。
4. 最后再做第 3 组 + 第 1 组一起混组复习。

这样一个大 group 内，每组都会被带入多次组合复习，但不是简单把 1+2+3 全部混在一起反复刷。

### 两个大 group 之间的节奏

两个大的 group 成对完成后，再做一次跨大 group 混组复习。

跨大 group 复习只按相邻成对触发：

- A + B
- C + D
- E + F

不会把已经完成跨组复习的 group 再拿来和下一个 group 反复组合。

例如：

- 大 group A = 第 1、2、3 小组。
- 大 group B = 第 4、5、6 小组。
- 大 group C = 第 7、8、9 小组。
- A 和 B 都完成后，进行一次 A + B 的跨大 group 混组复习。
- 如果此时 C 已完成，但 D 还没有出现，则不触发 C 的跨大 group 混组复习。
- 只有等 D 也完成后，才触发 C + D 的跨大 group 混组复习。

后续大 group 也按同样方式推进。

## 12. 播放与跟读规则

### 循环次数

- 播放循环次数是用户配置项。
- 默认一轮播放 3 次。
- 配置入口放在“我的”。

### 播放节奏

听音辨义 / 看中文回忆英文默认节奏：

**播放一次 -> 等待一次。**

用户可设置为：

**一次性播放 N 次 -> 再等待。**

设定次数内说对一次即可通过。

如果设定次数内一次没对：

- 自动展示答案
- 加入错词本
- 自动进入下一个

### 提前通过与继续强化

- 学生可以提前点击“已记住”跳过。
- 点击“还不熟”会立即再播放一轮。
- 一轮 = 用户设定次数，默认 3 次。
- “还不熟”不进入错词本。

### 跟读检测

- 默认鼓励跟读。
- 如果没有检测到声音，只轻提示一次。
- 提示可关闭。
- 不强制跟读。
- 允许用户默读，也允许用户只靠耳朵和嘴巴学习。

## 13. 三个验收环节的统一规则

适用环节：

- 本组复习
- 听音辨义
- 看中文回忆英文
- 混组复习

通过方式：

1. 收到语音输入且正确。
2. 点击“已记住”。

语音正确规则：

- 中文释义：命中任意一个中文释义即可。
- 英文回忆：英文发音识别正确即可。

点击“已记住”：

- 视为通过。
- 不进入错词本。
- 记录为“自评通过”，后续统计可信度低一些。

超时 / 设定次数内未通过：

- 自动展示答案。
- 加入错词本。
- 自动进入下一个。

## 14. 不再作为主逻辑的内容

以下内容不再作为默认产品主逻辑：

- 艾宾浩斯遗忘曲线排程
- 到期复习优先级排序
- 因超过复习窗口而降级
- 长周期复习节点

这些可以以后作为高级策略重新讨论，但 v2 主线先完全移除。
