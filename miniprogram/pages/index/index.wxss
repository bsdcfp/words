.app-shell {
  min-height: 100vh;
  background: transparent;
  overflow-x: hidden;
}

.view {
  width: 100%;
  max-width: 720px;
  min-height: 100vh;
  margin: 0 auto;
  padding: calc(env(safe-area-inset-top) + 32rpx) 28rpx calc(env(safe-area-inset-bottom) + 184rpx);
  box-sizing: border-box;
}

.topbar {
  min-height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  margin-bottom: 32rpx;
}

.topbar.compact {
  justify-content: flex-start;
}

.sticky-view {
  padding-top: calc(env(safe-area-inset-top) + 144rpx);
}

.sticky-view .topbar {
  position: fixed;
  top: 0;
  left: 50%;
  z-index: 8;
  width: 100%;
  max-width: 720px;
  min-height: calc(env(safe-area-inset-top) + 112rpx);
  margin: 0;
  padding: calc(env(safe-area-inset-top) + 16rpx) 28rpx 16rpx;
  transform: translateX(-50%);
  background:
    linear-gradient(180deg, rgba(250, 249, 245, 0.98) 0%, rgba(250, 249, 245, 0.9) 72%, rgba(250, 249, 245, 0) 100%);
  box-sizing: border-box;
  backdrop-filter: blur(12rpx);
}

.page-title,
.hero-title,
.test-word,
.study-word,
.detail-word {
  display: block;
  color: #141413;
  font-weight: 900;
  line-height: 1.18;
}

.page-title {
  font-size: 44rpx;
}

.eyebrow,
.section-label,
.muted,
.hint,
.option-sub {
  display: block;
  color: #6e6a62;
}

.eyebrow,
.section-label {
  margin-bottom: 12rpx;
  font-size: 26rpx;
  font-weight: 800;
}

.hero-panel,
.question-panel,
.result-panel,
.study-card,
.audio-panel,
.report-panel,
.empty-state,
.detail-card {
  background: rgba(250, 249, 245, 0.72);
  border: 2rpx solid rgba(20, 20, 19, 0.12);
  border-radius: 16rpx;
  padding: 36rpx;
  box-shadow: 0 28rpx 68rpx rgba(20, 20, 19, 0.16);
  box-sizing: border-box;
}

.hero-title {
  max-width: 100%;
  margin-bottom: 36rpx;
  font-size: 48rpx;
  word-break: break-word;
}

.primary-button,
.secondary-button,
.list-button,
.option-card,
.icon-button,
.text-link,
.segmented-button,
.sound-button,
.speaker-tile,
.sound-mini,
.select-toggle,
.word-link {
  border: 0;
  line-height: 1.2;
  margin: 0;
  box-sizing: border-box;
  max-width: 100%;
}

button::after {
  border: 0;
}

.primary-button,
.secondary-button {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 0;
  min-height: 96rpx;
  border-radius: 16rpx;
  padding: 0 36rpx;
  font-weight: 900;
}

.primary-button {
  background: #d97757;
  color: #141413;
}

.primary-button[disabled] {
  opacity: 0.45;
}

.primary-button.disabled {
  opacity: 0.38;
}

.primary-button.small {
  min-height: 84rpx;
}

.hero-panel .primary-button,
.result-panel .primary-button,
.report-panel .primary-button,
.empty-state .primary-button {
  min-width: 300rpx;
  margin: 0 auto;
}

.secondary-button {
  background: rgba(250, 249, 245, 0.82);
  color: #4f623c;
}

.icon-button {
  display: flex;
  flex: 0 0 84rpx;
  align-items: center;
  justify-content: center;
  width: 84rpx;
  min-width: 84rpx;
  max-width: 84rpx;
  height: 84rpx;
  padding: 0;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.54);
  color: #141413;
  font-size: 48rpx;
  text-align: center;
}

.topbar.compact .icon-button {
  flex: 0 0 84rpx;
  width: 84rpx;
  min-width: 84rpx;
  max-width: 84rpx;
}

.coach-strip,
.stats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18rpx;
  margin: 28rpx 0;
}

.stats-grid {
  grid-template-columns: repeat(3, 1fr);
}

.coach-strip view,
.metric,
.info-strip view,
.copy-card,
.review-summary {
  border: 2rpx solid rgba(20, 20, 19, 0.14);
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.56);
  padding: 24rpx;
  box-sizing: border-box;
}

.coach-strip text:first-child,
.strong,
.card-title {
  display: block;
  font-weight: 900;
}

.info-strip,
.action-list,
.option-stack,
.word-list,
.review-stack {
  display: grid;
  width: 100%;
  gap: 24rpx;
  margin: 28rpx 0;
}

.metric {
  text-align: center;
}

.metric text:first-child {
  display: block;
  font-size: 42rpx;
  font-weight: 900;
}

.metric text:last-child {
  display: block;
  color: #6e6a62;
  font-size: 24rpx;
}

.list-button {
  width: 100%;
  min-width: 0;
  justify-self: stretch;
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  min-height: 148rpx;
  gap: 24rpx;
  padding: 32rpx;
  border-radius: 16rpx;
  background: #faf9f5;
  text-align: left;
}

.action-entry {
  position: relative;
  grid-template-columns: minmax(0, 1fr) auto 42rpx;
  border: 2rpx solid rgba(217, 119, 87, 0.34);
  background:
    linear-gradient(135deg, rgba(217, 119, 87, 0.12), rgba(250, 249, 245, 0.9));
  box-shadow: 0 16rpx 34rpx rgba(20, 20, 19, 0.08);
}

.action-entry::before {
  content: "";
  position: absolute;
  left: 0;
  top: 24rpx;
  bottom: 24rpx;
  width: 8rpx;
  border-radius: 999rpx;
  background: #d97757;
}

.action-entry text:nth-child(2) {
  color: #6e6a62;
  font-size: 24rpx;
  text-align: right;
  word-break: break-word;
}

.action-entry-pressed {
  transform: scale(0.985);
  opacity: 0.86;
}

.list-button text:first-child {
  display: block;
  font-weight: 900;
}

.list-button text:last-child {
  display: block;
  color: #6e6a62;
  font-size: 24rpx;
  text-align: right;
  word-break: break-word;
}

.entry-arrow {
  display: flex !important;
  align-items: center;
  justify-content: center;
  width: 42rpx;
  height: 42rpx;
  border-radius: 999rpx;
  background: #d97757;
  color: #141413 !important;
  font-size: 34rpx !important;
  font-weight: 900;
  line-height: 1;
}

.progress-label {
  margin-left: auto;
  color: #6e6a62;
  font-weight: 900;
}

.test-word,
.study-word,
.detail-word {
  margin: 24rpx 0;
  font-size: 64rpx;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.option-card {
  display: block;
  width: 100%;
  min-height: 108rpx;
  padding: 28rpx;
  border-radius: 16rpx;
  background: #faf9f5;
  color: #141413;
  text-align: left;
  font-weight: 800;
  white-space: normal;
  overflow: hidden;
  word-break: break-word;
}

.test-option {
  display: flex;
  align-items: center;
  min-height: 118rpx;
  border: 2rpx solid rgba(120, 140, 93, 0.28);
  border-left: 10rpx solid #788c5d;
  background:
    linear-gradient(135deg, rgba(120, 140, 93, 0.16), rgba(232, 230, 220, 0.86));
  box-shadow: 0 14rpx 30rpx rgba(20, 20, 19, 0.08);
  font-size: 32rpx;
}

.test-option-pressed {
  transform: scale(0.985);
  opacity: 0.86;
}

.option-card.correct {
  background: rgba(120, 140, 93, 0.22);
  color: #4f623c;
}

.option-card.wrong {
  background: rgba(217, 119, 87, 0.22);
  color: #8f412b;
}

.option-card.muted-card {
  opacity: 0.45;
}

.option-pos {
  margin-right: 10rpx;
  color: #788c5d;
}

.unknown-button {
  width: 100%;
  margin-top: 24rpx;
}

.counter-bar,
.bottom-actions {
  position: fixed;
  left: 50%;
  bottom: calc(env(safe-area-inset-bottom) + 28rpx);
  width: calc(100vw - 56rpx);
  max-width: 720px;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 20rpx;
  border-radius: 18rpx;
  background: rgba(250, 249, 245, 0.94);
  box-shadow: 0 20rpx 48rpx rgba(20, 20, 19, 0.16);
  box-sizing: border-box;
}

.precheck-actions {
  display: grid;
  grid-template-columns: 92rpx minmax(0, 1fr) minmax(0, 1fr);
  align-items: center;
}

.precheck-view {
  position: relative;
  height: 100vh;
  min-height: 0;
  overflow: hidden;
  padding-bottom: 0;
}

.precheck-view .topbar {
  min-height: 68rpx;
  gap: 18rpx;
  margin-bottom: 16rpx;
}

.precheck-view .icon-button {
  flex-basis: 64rpx;
  width: 64rpx;
  min-width: 64rpx;
  max-width: 64rpx;
  height: 64rpx;
  border-radius: 16rpx;
  font-size: 38rpx;
}

.precheck-view .page-title {
  font-size: 38rpx;
  letter-spacing: -1rpx;
}

.precheck-scroll {
  position: absolute;
  top: calc(env(safe-area-inset-top) + 132rpx);
  right: 0;
  bottom: calc(env(safe-area-inset-bottom) + 154rpx);
  left: 0;
  height: auto;
  min-height: 0;
  overflow-x: hidden;
  box-sizing: border-box;
}

.precheck-view .word-list {
  width: 100%;
  box-sizing: border-box;
  gap: 14rpx;
  margin: 0;
  padding: 0 52rpx 22rpx;
}

.precheck-list {
  width: 100%;
  min-width: 0;
  overflow-x: hidden;
  box-sizing: border-box;
}

.precheck-view .precheck-actions {
  width: calc(100vw - 104rpx);
  max-width: 668px;
  grid-template-columns: 76rpx minmax(0, 1fr) minmax(0, 1fr);
  padding: 14rpx 16rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.2);
  border-radius: 24rpx;
  background: linear-gradient(135deg, rgba(20, 20, 19, 0.98), rgba(79, 98, 60, 0.94));
  box-shadow: 0 22rpx 54rpx rgba(20, 20, 19, 0.24);
}

.bottom-actions.two {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  align-items: stretch;
}

.bottom-actions.status-actions {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 176rpx;
  align-items: center;
}

.status-actions text {
  display: block;
  min-width: 0;
  color: #6e6a62;
  font-size: 26rpx;
  font-weight: 900;
  line-height: 1.25;
  word-break: break-word;
}

.bottom-actions .primary-button,
.bottom-actions .secondary-button {
  width: 100%;
  min-width: 0;
  max-width: 100%;
  padding: 0 20rpx;
  overflow: hidden;
}

.pill {
  min-width: 72rpx;
  padding: 12rpx 18rpx;
  border-radius: 999rpx;
  color: #141413;
  text-align: center;
  font-weight: 900;
}

.green {
  background: #788c5d;
}

.red {
  background: #d97757;
}

.blue {
  background: #6a9bcc;
}

.vocab-number {
  display: block;
  margin: 24rpx 0;
  font-size: 96rpx;
  font-weight: 900;
  color: #788c5d;
}

.precheck-row,
.review-card {
  padding: 28rpx;
  border: 2rpx solid rgba(20, 20, 19, 0.14);
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.62);
  box-sizing: border-box;
}

.precheck-row {
  display: grid;
  gap: 22rpx;
}

.precheck-view .precheck-row {
  width: 100%;
  min-width: 0;
  gap: 10rpx;
  padding: 16rpx 22rpx;
  border-color: rgba(20, 20, 19, 0.1);
  border-radius: 20rpx;
  background: rgba(255, 255, 255, 0.58);
  box-shadow: 0 14rpx 36rpx rgba(20, 20, 19, 0.08);
  overflow: hidden;
}

.precheck-head {
  display: grid;
  grid-template-columns: 44rpx minmax(0, 1fr);
  align-items: center;
  gap: 18rpx;
}

.precheck-view .precheck-head {
  grid-template-columns: 34rpx minmax(0, 1fr);
  gap: 16rpx;
  min-width: 0;
}

.precheck-row.picked {
  border-color: #788c5d;
}

.index {
  color: #6e6a62;
  font-weight: 900;
}

.precheck-view .index {
  font-size: 28rpx;
}

.precheck-main {
  display: flex;
  min-width: 0;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.select-toggle,
.sound-mini {
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  background: #faf9f5;
  color: #788c5d;
  font-weight: 900;
}

.select-toggle {
  width: 64rpx;
  height: 64rpx;
}

.precheck-view .select-toggle {
  width: 52rpx;
  height: 52rpx;
  font-size: 28rpx;
  box-shadow: 0 8rpx 20rpx rgba(120, 140, 93, 0.1);
}

.select-toggle.on {
  background: #d97757;
  color: #141413;
}

.sound-mini {
  width: 124rpx;
  min-width: 124rpx;
  height: 72rpx;
  font-size: 26rpx;
}

.word-link {
  display: block;
  min-width: 0;
  padding: 14rpx 28rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.84);
  text-align: center;
  font-size: 40rpx;
  font-weight: 900;
  overflow-wrap: anywhere;
  line-height: 1.15;
}

.precheck-view .word-link {
  flex: 0 1 auto;
  min-width: 0;
  padding: 8rpx 18rpx;
  background: rgba(250, 249, 245, 0.96);
  color: #141413;
  font-size: 34rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.meaning-inline {
  display: block;
  padding-left: 62rpx;
  color: #6e6a62;
  font-size: 32rpx;
  font-weight: 800;
  word-break: break-word;
}

.meaning-inline text {
  margin-right: 10rpx;
  color: #788c5d;
}

.precheck-view .meaning-inline {
  padding-left: 48rpx;
  font-size: 25rpx;
  line-height: 1.3;
}

.precheck-view .precheck-meaning {
  flex: 1 1 auto;
  min-width: 0;
  padding-left: 0;
  color: #4f623c;
  font-size: 28rpx;
  font-weight: 900;
  text-align: right;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.precheck-view .precheck-meaning text {
  color: #d97757;
}

.segmented {
  grid-column: 1 / -1;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;
}

.precheck-view .segmented {
  display: flex;
  justify-content: flex-end;
  gap: 14rpx;
  min-width: 0;
}

.segmented-button {
  display: flex;
  width: 100%;
  min-width: 0;
  min-height: 72rpx;
  align-items: center;
  justify-content: center;
  border-radius: 14rpx;
  background: #faf9f5;
  color: #6e6a62;
  font-weight: 800;
  box-sizing: border-box;
}

.precheck-view .segmented-button {
  width: auto;
  min-width: 104rpx;
  min-height: 54rpx;
  padding: 0 22rpx;
  border-radius: 18rpx;
  border: 2rpx solid transparent;
  font-size: 26rpx;
  font-weight: 900;
}

.precheck-view .segmented-button.known {
  border-color: rgba(120, 140, 93, 0.28);
  background: rgba(120, 140, 93, 0.12);
  color: #4f623c;
}

.precheck-view .segmented-button.unfamiliar {
  border-color: rgba(217, 119, 87, 0.3);
  background: rgba(217, 119, 87, 0.12);
  color: #8f412b;
}

.segmented .selected {
  background: rgba(120, 140, 93, 0.22);
  color: #4f623c;
}

.segmented .danger {
  background: rgba(217, 119, 87, 0.22);
  color: #8f412b;
}

.selection-count {
  display: block;
  color: #141413;
  font-size: 28rpx;
  font-weight: 900;
  line-height: 1.25;
  text-align: center;
}

.precheck-view .selection-count {
  padding: 8rpx 6rpx;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.14);
  color: #fff;
  font-size: 23rpx;
}

.precheck-view .bottom-actions .primary-button,
.precheck-view .bottom-actions .secondary-button {
  min-height: 72rpx;
  font-size: 28rpx;
}

.precheck-view .bottom-actions .secondary-button {
  background: rgba(255, 255, 255, 0.92);
  color: #4f623c;
}

.precheck-view .bottom-actions .primary-button {
  background: #d97757;
  color: #141413;
}

.precheck-view .bottom-actions .primary-button.disabled {
  background: rgba(255, 255, 255, 0.3);
  color: rgba(255, 255, 255, 0.68);
  opacity: 1;
}

.memory-image {
  display: grid;
  gap: 10rpx;
  margin: 24rpx 0;
  padding: 28rpx;
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(120, 140, 93, 0.16), rgba(217, 119, 87, 0.2));
}

.memory-mark {
  color: #4f623c;
  font-weight: 900;
}

.study-memory-image {
  margin: 4rpx 0 0;
  border: 2rpx solid rgba(120, 140, 93, 0.18);
  background:
    linear-gradient(135deg, rgba(120, 140, 93, 0.12), rgba(217, 119, 87, 0.14));
}

.speaker-tile {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 92rpx;
  margin: 16rpx 0;
  border-radius: 999rpx;
  background: #141413;
  color: #faf9f5;
  font-weight: 900;
}

.sound-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 148rpx;
  min-height: 58rpx;
  margin: 4rpx 0 8rpx;
  padding: 0 26rpx;
  border: 2rpx solid rgba(120, 140, 93, 0.22);
  border-radius: 999rpx;
  background: rgba(120, 140, 93, 0.1);
  color: #4f623c;
  font-size: 25rpx;
  font-weight: 900;
}

.speaker-tile {
  width: 200rpx;
  height: 200rpx;
  border-radius: 56rpx;
  font-size: 36rpx;
}

.ipa {
  display: block;
  margin: 10rpx 0;
  color: #6e6a62;
}

.meaning {
  display: block;
  margin: 16rpx 0;
  font-size: 34rpx;
  font-weight: 900;
  word-break: break-word;
}

.meaning text {
  margin-right: 12rpx;
  color: #788c5d;
}

.text-link {
  display: inline-block;
  padding: 0;
  background: transparent;
  color: #4f623c;
  text-align: left;
  font-weight: 900;
}

.centered {
  display: block;
  margin: 24rpx auto 0;
}

.review-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.detail-entry {
  border-color: rgba(120, 140, 93, 0.18);
  background:
    linear-gradient(135deg, rgba(250, 249, 245, 0.88), rgba(232, 230, 220, 0.72));
}

.review-card-pressed {
  transform: scale(0.985);
  opacity: 0.88;
}

.review-copy {
  flex: 1;
  min-width: 0;
}

.review-card text {
  display: block;
  min-width: 0;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.review-summary {
  display: grid;
  gap: 8rpx;
  margin-bottom: 24rpx;
}

.report-ring {
  display: flex;
  width: 220rpx;
  height: 220rpx;
  align-items: center;
  justify-content: center;
  margin: 0 auto 24rpx;
  border-radius: 999rpx;
  background: #788c5d;
  color: #141413;
  font-size: 56rpx;
  font-weight: 900;
}

.modal-mask {
  position: fixed;
  inset: 0;
  z-index: 10;
  padding: calc(env(safe-area-inset-top) + 64rpx) 28rpx calc(env(safe-area-inset-bottom) + 28rpx);
  background: rgba(20, 20, 19, 0.42);
  box-sizing: border-box;
}

.transition-mask {
  position: fixed;
  inset: 0;
  z-index: 9;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 48rpx;
  background: rgba(20, 20, 19, 0.18);
  box-sizing: border-box;
}

.transition-card {
  display: grid;
  gap: 10rpx;
  min-width: 0;
  width: calc(100vw - 104rpx);
  max-width: 616px;
  padding: 36rpx 40rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.5);
  border-radius: 24rpx;
  background: rgba(250, 249, 245, 0.94);
  box-shadow: 0 24rpx 72rpx rgba(20, 20, 19, 0.22);
  text-align: center;
  box-sizing: border-box;
}

.transition-card text:first-child {
  color: #141413;
  font-size: 32rpx;
  line-height: 1.3;
  font-weight: 900;
  word-break: break-word;
}

.transition-card text:last-child {
  color: #6e6a62;
  font-size: 26rpx;
  line-height: 1.3;
  font-weight: 800;
  word-break: break-word;
}

.notice-mask {
  background: rgba(20, 20, 19, 0.18);
}

.detail-card {
  position: relative;
  width: 100%;
  max-width: 680px;
  max-height: 84vh;
  margin: 0 auto;
  overflow: auto;
  background: #faf9f5;
}

.close {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
}

.empty-state {
  display: grid;
  gap: 24rpx;
  text-align: center;
}

.home-view,
.profile-view {
  padding-bottom: calc(env(safe-area-inset-bottom) + 148rpx);
}

.profile-chip,
.level-pill {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 64rpx;
  padding: 0 28rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.58);
  color: #141413;
  font-size: 26rpx;
  font-weight: 900;
}

.plan-panel,
.profile-hero,
.profile-card,
.level-panel,
.study-focus-card,
.boot-error-card {
  width: 100%;
  min-width: 0;
  border: 2rpx solid rgba(20, 20, 19, 0.12);
  border-radius: 24rpx;
  background: rgba(250, 249, 245, 0.72);
  box-shadow: 0 24rpx 60rpx rgba(20, 20, 19, 0.13);
  box-sizing: border-box;
}

.plan-panel {
  display: grid;
  gap: 24rpx;
  padding: 40rpx 36rpx;
}

.boot-error-card {
  display: grid;
  gap: 18rpx;
  margin-bottom: 24rpx;
  padding: 28rpx;
  background: rgba(255, 255, 255, 0.72);
}

.plan-panel .hero-title {
  margin-bottom: 0;
}

.plan-copy {
  display: block;
  color: #665d55;
  font-size: 28rpx;
  font-weight: 700;
  line-height: 1.5;
}

.plan-panel .primary-button {
  width: 100%;
  margin: 6rpx 0 0;
}

.plan-grid,
.profile-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16rpx;
  margin: 24rpx 0;
}

.plan-grid view,
.profile-grid view {
  min-width: 0;
  padding: 24rpx 12rpx;
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.48);
  text-align: center;
  box-sizing: border-box;
}

.plan-grid text:first-child,
.profile-grid text:first-child {
  display: block;
  color: #141413;
  font-size: 42rpx;
  font-weight: 900;
}

.plan-grid text:last-child,
.profile-grid text:last-child {
  display: block;
  color: #6e6a62;
  font-size: 23rpx;
  font-weight: 800;
}

.today-card {
  display: grid;
  gap: 16rpx;
}

.today-card view {
  padding: 26rpx;
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.42);
  box-sizing: border-box;
}

.tab-bar {
  position: fixed;
  left: 50%;
  bottom: calc(env(safe-area-inset-bottom) + 22rpx);
  z-index: 7;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10rpx;
  width: calc(100vw - 56rpx);
  max-width: 680px;
  padding: 10rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.28);
  border-radius: 24rpx;
  background: rgba(20, 20, 19, 0.94);
  box-shadow: 0 20rpx 54rpx rgba(20, 20, 19, 0.24);
  transform: translateX(-50%);
  box-sizing: border-box;
}

.tab-item {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 76rpx;
  border-radius: 18rpx;
  color: rgba(250, 249, 245, 0.78);
  font-weight: 900;
}

.tab-item.active {
  background: #d97757;
  color: #141413;
}

.profile-hero {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
  padding: 34rpx;
}

.profile-book {
  display: block;
  margin-bottom: 10rpx;
  color: #141413;
  font-size: 42rpx;
  font-weight: 900;
}

.profile-grid {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.profile-card {
  display: grid;
  gap: 24rpx;
  padding: 30rpx;
}

.action-card {
  transition: transform 120ms ease, background 120ms ease;
}

.card-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.week-row {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 10rpx;
}

.week-dot {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 76rpx;
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.56);
  color: #6e6a62;
  font-weight: 900;
}

.week-dot text:first-child {
  font-size: 22rpx;
}

.week-dot text:last-child {
  font-size: 26rpx;
}

.week-dot.active {
  background: rgba(120, 140, 93, 0.16);
  color: #4f623c;
}

.week-summary {
  margin-top: -6rpx;
  font-size: 23rpx;
}

.profile-list {
  display: grid;
  gap: 18rpx;
  margin-top: 24rpx;
}

.month-progress-view {
  min-height: 100vh;
}

.month-panel {
  display: grid;
  gap: 22rpx;
  width: 100%;
  min-width: 0;
  padding: 30rpx;
  border: 2rpx solid rgba(20, 20, 19, 0.12);
  border-radius: 24rpx;
  background: rgba(250, 249, 245, 0.72);
  box-shadow: 0 24rpx 60rpx rgba(20, 20, 19, 0.13);
  box-sizing: border-box;
}

.month-picker {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  min-height: 84rpx;
  padding: 0 24rpx;
  border-radius: 22rpx;
  background: rgba(255, 255, 255, 0.56);
  color: #141413;
  font-size: 30rpx;
  font-weight: 900;
}

.month-picker text:last-child {
  color: #6e6a62;
  font-size: 24rpx;
}

.month-switch {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
  align-items: center;
}

.month-switch view {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 64rpx;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.46);
  color: #4f623c;
  font-size: 24rpx;
  font-weight: 900;
}

.month-switch view:nth-child(2) {
  color: #141413;
}

.month-summary {
  grid-template-columns: repeat(3, minmax(0, 1fr));
  margin: 0;
}

.month-calendar {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 10rpx;
}

.month-weekday {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 42rpx;
  color: #6e6a62;
  font-size: 22rpx;
  font-weight: 900;
}

.month-day {
  display: flex;
  min-height: 76rpx;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.5);
  color: #6e6a62;
  font-size: 24rpx;
  font-weight: 900;
}

.month-day.blank {
  background: transparent;
}

.month-day.active {
  background: rgba(120, 140, 93, 0.18);
  color: #4f623c;
}

.month-day-meta {
  margin-top: 2rpx;
  font-size: 18rpx;
}

.level-panel {
  display: grid;
  gap: 30rpx;
  padding: 34rpx;
}

.level-group {
  display: grid;
  gap: 16rpx;
}

.level-title {
  color: #6e6a62;
  font-size: 28rpx;
  font-weight: 900;
}

.level-options {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14rpx;
}

.level-option {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 74rpx;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.78);
  color: #141413;
  font-size: 26rpx;
  font-weight: 900;
}

.study-view {
  padding-bottom: calc(env(safe-area-inset-bottom) + 168rpx);
}

.study-focus-card {
  display: grid;
  gap: 20rpx;
  padding: 38rpx 34rpx;
}

.study-focus-card .study-word {
  margin: 18rpx 0 0;
  font-size: 72rpx;
  letter-spacing: -2rpx;
}

.study-focus-card .meaning {
  margin: 0;
}

.study-tool-row {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
  margin-top: 16rpx;
}

.tool-button {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 66rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.7);
  color: #665d55;
  font-size: 25rpx;
  font-weight: 900;
}

.tool-button.active,
.tool-button.primary {
  background: #788c5d;
  color: #141413;
}

.tool-button.primary {
  background: #d97757;
}

.audio-view {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding-bottom: calc(env(safe-area-inset-bottom) + 168rpx);
}

.audio-minimal {
  display: grid;
  gap: 28rpx;
  padding: 38rpx 34rpx;
}

.audio-minimal .speaker-tile {
  width: 168rpx;
  height: 168rpx;
  min-height: 168rpx;
  margin: 0 auto;
  border-radius: 44rpx;
  background: rgba(250, 249, 245, 0.88);
  color: #4f623c;
  box-shadow: 0 18rpx 42rpx rgba(120, 140, 93, 0.14);
  font-size: 30rpx;
}

.audio-minimal .option-stack {
  margin: 0;
  gap: 18rpx;
}

.audio-tools {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16rpx;
}

.audio-tools view {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 68rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.56);
  color: #4f623c;
  font-weight: 900;
}

@media (max-width: 360px) {
  .view {
    padding-left: 22rpx;
    padding-right: 22rpx;
  }

  .hero-panel,
  .question-panel,
  .result-panel,
  .study-card,
  .audio-panel,
  .report-panel,
  .empty-state,
  .detail-card {
    padding: 28rpx;
  }

  .hero-title {
    font-size: 42rpx;
  }

  .page-title {
    font-size: 40rpx;
  }

  .stats-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .list-button {
    align-items: stretch;
    flex-direction: column;
  }

  .bottom-actions .primary-button,
  .bottom-actions .secondary-button {
    width: 100%;
  }

  .test-word,
  .study-word,
  .detail-word {
    font-size: 56rpx;
  }
}

@media (min-width: 520px) {
  .view {
    padding-left: 40rpx;
    padding-right: 40rpx;
  }

  .sticky-view .topbar {
    padding-left: 40rpx;
    padding-right: 40rpx;
  }

  .hero-title {
    font-size: 56rpx;
  }

  .page-title {
    font-size: 48rpx;
  }

  .hero-panel,
  .question-panel,
  .result-panel,
  .study-card,
  .audio-panel,
  .report-panel,
  .empty-state,
  .detail-card {
    padding: 44rpx;
  }

  .coach-strip {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .info-strip {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}
