This folder contains local UI icons used by the Mini Program.

Icons are intentionally stored as small local SVG assets so page markup can use
`<image>` consistently instead of relying on emoji or CSS-drawn shapes. Keep
filenames semantic and add only icons that are actually used by the app.
