page {
  min-height: 100vh;
  background: #faf9f5;
  color: #141413;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

button {
  font: inherit;
  margin: 0;
  box-sizing: border-box;
}

button::after {
  border: 0;
}
